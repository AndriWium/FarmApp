# Decisions

One line per irreversible-ish decision, dated.

- 2026-09-15: Swagger bearer-auth padlock (doc 13 Phase 0a) is wired via Swashbuckle's own `AddSwaggerGen`/`UseSwagger` document generation instead of the previously-used `Microsoft.AspNetCore.OpenApi` `AddOpenApi`/`MapOpenApi`. Reason: .NET 10 ships `Microsoft.OpenApi` 2.x, which removed the concrete `OpenApiSecurityScheme`/`OpenApiReference` types `AddOpenApi`'s document-transformer API expected; Swashbuckle.AspNetCore 10.2.3 already targets the new model with a stable, documented `AddSecurityDefinition`/`AddSecurityRequirement` API (the latter now takes an `OpenApiSecuritySchemeReference` via a `Func<OpenApiDocument, OpenApiSecurityRequirement>` delegate). Swagger UI now serves from `/swagger/v1/swagger.json` instead of `/openapi/v1.json`. No spec doc was wrong — doc 13 just said "standard ASP.NET Core JWT+Swashbuckle setup," which this now literally is.
- 2026-09-15 (Phase 0b-1): `InputItem.Category` is the first enum in the codebase. Stored `.HasConversion<string>().HasMaxLength(20)` per doc 11. Also registered `JsonStringEnumConverter` globally in `Program.cs` (`AddJsonOptions`) so the API/Swagger layer serializes enums as readable strings too, not just the DB column — otherwise System.Text.Json's default numeric enum serialization would contradict the "strings for report readability" intent one layer up. Future enums inherit this for free.
- 2026-09-15 (Phase 0b-1): field caps not spelled out in doc 02/the task brief, chosen consistent with existing precedent (Grade/Block `Name` = 50, `Block.Note` = 500): `InputItem.Unit` = `HasMaxLength(20)` (e.g. "kg", "L", "bag", "each"); `PriceList.Name` = `HasMaxLength(50)`; `Supplier.Name`/`Supplier.Phone` = `HasMaxLength(50)`, `Supplier.Phone` optional.
- 2026-09-15 (Phase 0b-2): `PackSize` has no `IsActive` column - per the doc brief's own suggestion, a pack size is small enough that a mistake is just deleted and recreated, so `PackSizeService.DeleteAsync` performs a real `DELETE`, the first hard-delete in the codebase (everything else is soft-delete). Scoped uniqueness `(ProductId, Name)` still enforced via a unique index.
- 2026-09-15 (Phase 0b-2): `Product.CropId`/`Product.MakeMode` are nullable and not hard-restricted to their "natural" `ProductType` (e.g. nothing stops a `CropId` on a `Resale` product) - the doc brief explicitly calls this out as real-world nuance a human should be able to override, so only the referential-integrity check (CropId must reference a real Crop, when provided) is enforced.
- 2026-09-15 (Phase 0b-2): `Price.PackSizeId` made nullable (per brief) to support "price per base unit, no specific pack" (loose kg pricing). A filtered unique index `IX_Prices_ActiveCombo` on `(PriceListId, ProductId, GradeId, PackSizeId) WHERE ValidTo IS NULL` enforces at the DB level that at most one price row is "current" per combination, as a backstop to `SetPriceAsync`'s own read-then-close-then-insert logic.
- 2026-09-15 (Phase 0b-2): `Price` write path exposes only `SetPriceAsync` (upsert-with-history) via `POST /api/v1/prices` - deliberately no `PUT`/`DELETE`, since prices are never updated or deleted, only superseded. Reads are `GET /api/v1/prices/current` and `GET /api/v1/prices/history`, both taking `priceListId`/`productId`/`gradeId`/`packSizeId` as query params (not personal data, so query params are fine per doc 11). `ValidFrom`/`ValidTo` use `DateOnly` (mapped to SQL `date`) - the EF Core/SQL Server provider handled it cleanly, no friction.
- 2026-09-15 (Phase 0b-2): `RecipeLine` is modeled as a sub-resource of `Product` (`IProductService.SetRecipeAsync`/`GetWithRecipeAsync`, `PUT`/`GET /api/v1/products/{id}/recipe`), not a standalone CRUD - matches the doc brief. It has its own `IRecipeLineRepository` in Domain (repository pattern still applies internally) but no controller of its own. Also added a unique index on `(ProductId, InputItemId)` (not spelled out in the brief) - the same ingredient listed twice in one recipe would silently double-count cost/depletion with no legitimate use case, so it's blocked at the DB level.
- 2026-09-15 (Phase 1a): `StockMovement` implements `IPeriodLocked` (`BusinessDate => Date`, explicit interface implementation). `PeriodLockInterceptor` was scaffolded in Phase 0a explicitly anticipating "Sale and StockMovement (Phase 1/2) will be the first" - wiring it in now means no later migration/retrofit is needed and matches the codebase's own stated intent. Cost: none (BusinessDate is computed, not a persisted column) - a movement dated into a closed accounting month is now rejected at save time.
- 2026-09-15 (Phase 1a): the movement-recording service (`IStockMovementService`) covers only outflow/depletion movement types (Wastage, OwnUse, Sample, Donation, Adjustment, Repack, TransferOut+TransferIn) - all FIFO-allocated across existing batches via `IStockAllocationService`. `Adjustment` here is specifically a *downward* adjustment (stock-take shortfall/shrinkage): a *positive* "found extra stock" adjustment isn't supported by this endpoint, since FIFO-allocating a positive quantity across existing batches doesn't make sense (a positive adjustment would need to create a new `StockBatch`, which is a different operation, out of scope until Phase 1's own StockTake feature is built). `Repack` is likewise modeled as a plain negative depletion (stock leaving its current tracked form/pack size) - the brief's request shape for Repack (ProductId/GradeId/Qty/Reason/LocationId, identical to Wastage) doesn't carry a "target product/pack size," so no offsetting "repack-in" movement is created this phase.
- 2026-09-15 (Phase 1a): added `POST/GET /api/v1/stock-batches` (`StockBatchesController`/`StockBatchService`) as a direct-creation endpoint for `StockBatch`, restricted to `Source: Harvest|Purchase` (rejects `Production` - `ProductionBatch` needs `StockMovement` to exist first, which the task brief itself calls circular). This is a documented stopgap: Phase 1b's real purchase-in/harvest-in use cases will create batches through their own flows. Creating a batch also seeds one initial `HarvestIn`/`PurchaseIn` movement for `QtyIn` in the same request, since on-hand is always derived from the movement ledger (doc 02) - a batch with no movement would show zero on hand despite a non-zero `QtyIn`. Because `StockMovement.StockBatchId` has no navigation property (matches the rest of the codebase's loose-FK convention), the batch is saved first to materialize its id, then the seeding movement is added and saved again - two `SaveChangesAsync` calls, not the "one atomic call" the brief specifies for the *movement-recording* service (which never creates new batches, so never hits this problem).
- 2026-09-15 (Phase 1a): `StockMovementsController`'s six depletion endpoints (wastage/own-use/sample/donation/adjustment/repack) and `/transfer` all require `CanManageMasterData` (no `CanManageStock` policy exists yet, per the task brief's own instruction not to invent new auth policies this phase). A dedicated `CanManageStock` policy would be more precise long-term and is worth adding once Phase 2's till/POS roles clarify who should be allowed to record stock movements day-to-day.
- 2026-09-15 (Phase 1a): `IStockMovementRepository.GetOnHandSummaryAsync`'s Product/Grade grouping is done **client-side** (materializes the flat movement/batch/product/grade join via `ToListAsync`, then `GroupBy`/`Sum` in memory) rather than server-side. EF Core 10 cannot translate a `GroupBy` + `Sum` whose key/aggregate expressions reach back through multiple joins' transparent identifiers directly into a record constructor (`InvalidOperationException: ... could not be translated`); splitting it into a "flat projection, then GroupBy" over the *same* `IQueryable` doesn't help either, since nothing forces evaluation in between - it's still one expression tree at translation time. Materializing first is the simplest fix and is fine at this phase's data volumes (a small farm's whole movement ledger); if this becomes a real report over a large ledger, doc 04's `Reporting.StockOnHand` view (raw SQL/Dapper, bypassing repositories entirely) is the intended long-term answer per doc 11.
- 2026-09-15 (Phase 1b): `ProducePurchaseLine` carries exactly doc 02's field list (`ProducePurchaseLineId, ProducePurchaseId, ProductId, GradeId, Qty, UnitCost`) - no `ShelfLifeDays` column, since it's not in that list. But building the line's `StockBatch` (via `IStockBatchService.CreateAsync`, reused rather than duplicated) requires a `ShelfLifeDays > 0`, and nothing upstream (`Product`, `Grade`) carries a default shelf life to fall back on. `CreatePurchaseLineRequest` (the API request DTO, not the persisted entity) therefore carries `ShelfLifeDays` as a required per-line input that flows straight into the batch it creates and is never persisted on `ProducePurchaseLine` itself. `IProducePurchaseService.CreatePurchaseAsync` validates the supplier and every line's product/grade *before* writing anything (header insert, line inserts, and per-line batch+movement creation are three separate save points - the two-phase-save stopgap `StockBatchService.CreateAsync` already established, extended to three phases here since `ProducePurchaseLineId` must exist before it can become `StockBatch.PurchaseLineId`), so a bad reference is rejected up front rather than leaving a partially-built purchase.
- 2026-09-15 (Phase 1b): `StockMovementService` gained `RecordBatchAdjustmentAsync(stockBatchId, signedQty, reason, locationId, ct)` - a single signed `Adjustment` movement against one already-known batch, no FIFO allocation (the caller, `StockTakeService`, already knows exactly which batch a stock-take line counts, so there's no product/grade to allocate across). This is additive: the existing `RecordAsync(StockMovementType.Adjustment, ...)` path behind `POST /api/v1/stock-movements/adjustment` is untouched and stays downward-only/FIFO-allocated across a product/grade, per the task brief's instruction to leave the six existing depletion endpoints as they are. `RecordBatchAdjustmentAsync` deliberately does **not** call `SaveChangesAsync` - it adds the movement to the tracked `IStockMovementRepository` and lets the caller control the transaction boundary, since `StockTakeService.RecordCountsAsync` needs one atomic save covering every line's `CountedQty`/`Variance` update *and* every resulting adjustment movement across a whole stock take, not one save per line.
- 2026-09-15 (Phase 1b): `StockTake`/`StockTakeLine` never create a `StockBatch` - doc 02's `ProductId/BatchId` notation for the line means each line counts a specific existing `StockBatchId` (received earlier via harvest-in or purchase-in), so both directions of variance become a single signed `Adjustment` against that same batch. `StartStockTakeAsync` validates every given `StockBatchId` (and `LocationId`, if given) exists before writing anything, then snapshots each line's `SystemQty` from `IStockMovementRepository.GetOnHandAsync` at start time - never trusted from the client. `RecordCountsAsync` re-validates every `StockTakeLineId` belongs to the given stock take before mutating any of them, then sets `CountedQty`/`Variance` and - for non-zero variances - calls the new `RecordBatchAdjustmentAsync` per line, committing every line update and every movement in one `SaveChangesAsync`.
- 2026-09-15 (Phase 1b): `StockTakeService.RecordCountsAsync` is re-callable against the same stock-take lines - nothing rejects a second `POST .../counts` call naming an already-counted line. Each call computes `Variance` against the line's original `SystemQty` snapshot (never against a running total), so re-recording with a corrected count produces a second, independent adjustment movement rather than a delta. Acceptable for this phase - none of Phase 1's endpoints have `ClientGuid`/idempotency wired up yet (that's doc 08, deferred to Phase 2's `Sale`) - but worth tightening later (reject a second count on an already-counted line, or require an explicit re-open) once a real counting workflow shows whether recounts are a normal correction path or a mistake worth guarding against.
- 2026-09-15 (Phase 2a): `IUnitOfWork` gained `BeginTransactionAsync`/`CommitTransactionAsync`/`RollbackTransactionAsync`, implemented by `FarmAppDbContext` wrapping `Database.BeginTransactionAsync()` (an `IDbContextTransaction` kept in a private field - not exposed through the interface, so Domain/Application code depending on `IUnitOfWork` never needs an EF Core reference). This is a genuinely new, better pattern for this codebase, used for the first time by `SaleService.CreateSaleAsync`: everything from the `Sale` header insert through every line's stock depletion, every `SaleLine`, and every `SalePayment` now lives inside one real database transaction, so a mid-operation failure (most notably `InsufficientStock` on a later line) rolls back *everything*, including the header's own already-flushed `SaveChangesAsync` - not just whatever was still sitting unflushed in the change tracker. This is a materially stronger guarantee than `ProducePurchaseService`/`StockBatchService`/`StockTakeService`'s older two-phase-save stopgap (sequential `SaveChangesAsync` calls with only pre-validation narrowing the failure window - see the Phase 1a/1b entries above) - those three would benefit from the same treatment, but retrofitting them is out of this phase's scope; worth doing once a POS-scale write volume makes their existing failure window worth closing too.
- 2026-09-15 (Phase 2a): `TillSessionService.OpenAsync`/`SaleService.CreateSaleAsync` take the acting user's id (`openedByUserId`)/derive nothing user-related respectively as a plain method parameter rather than injecting `IHttpContextAccessor` into the `Application` layer. `TillSessionsController.Open` reads `ClaimTypes.NameIdentifier` off the authenticated `ClaimsPrincipal` itself and passes it in - keeping the JWT-claims dependency in Presentation, where doc 11 says HTTP-specific concerns belong, rather than reaching for `IHttpContextAccessor` the way `AuditInterceptor` (Infrastructure, a different layer with different rules) already does. Functionally identical to the brief's suggestion, just drawn on the Presentation/Application side of the line instead.
- 2026-09-15 (Phase 2a): `StockMovementService` gained `RecordSaleDepletionAsync(productId, gradeId, qtyBaseUnits, saleId, locationId, ct)` - the `SaleOut` counterpart to `RecordBatchAdjustmentAsync` rather than an extension of `RecordAsync`'s six-type switch (same "don't generalize the existing six, add a new method shaped like them" instruction as Phase 1a). It fetches each FIFO-allocated batch's `UnitCost` (`IStockBatchRepository.GetUnitCostsByIdsAsync`, new) and returns both the raw per-batch allocations (`SaleDepletionResult.Allocations`) and the resulting movement DTOs - it deliberately does **not** compute the weighted-average cost itself (that's `SaleService`'s job, via the Domain's `ISaleLineCalculator.WeightedAverageCost`), keeping "what batches did this touch, at what cost" (a stock-movement concern) separate from "what does a sale line's cost snapshot become" (a sale concern). Like `RecordBatchAdjustmentAsync`, it does not call `SaveChangesAsync` itself - `SaleService` owns the transaction boundary for the whole sale. Movements are tagged `RefTable: "Sale"`/`RefId: saleId` - the first real use of `StockMovement.RefTable`/`RefId`, which every prior movement-writing path has left null.
- 2026-09-15 (Phase 2a): the pack-size-to-base-unit conversion and the sale-line weighted-average-cost calculation both live in `FarmApp.Domain.Services.SaleLineCalculator` (interface `ISaleLineCalculator`) rather than inline in `SaleService` - same "costing is Domain, not Api" precedent as `IStockAllocationService`, and per the task brief's own instruction to put arithmetic this easy to get subtly wrong somewhere unit-testable. Both are covered in `tests/FarmApp.Domain.Tests/Services/SaleLineCalculatorTests.cs`, including the "spans batches at different costs" case that a naive first-batch-only or plain-average implementation would get wrong.
- 2026-09-15 (Phase 2a): four new `ServiceError` cases (`TillSessionAlreadyOpen`, `TillSessionClosed`, `PaymentMismatch`, `AccountPaymentRequiresCustomer`) were added rather than reusing `NotFound`/`DuplicateName` - each is a distinct business rule with its own 409 message in `ApiControllerBase`, and none of the existing cases fit without being misleading. Every other new failure mode in `SaleService.CreateSaleAsync` (missing till session/customer/product/grade/pack-size, or a pack size that exists but belongs to a different product) reuses plain `NotFound`, matching `ProducePurchaseService`'s precedent of a single generic entity-name string in the controller rather than a per-field detail message.
- 2026-09-15 (Phase 2a): `TillSession`/`Sale`'s day-close and refund/balance features are explicitly out of scope this phase (task brief): `TillSession.SystemCardTotal`/`CardMachineBatchTotal`/`Difference`/`DifferenceNote` are present as nullable columns (doc 02's field list) but nothing writes them yet - there is no `CloseAsync`. `Sale.Status` is always written as `Complete`; nothing transitions a sale to `Refunded` yet. A customer's outstanding balance is never stored anywhere (`SalePayment` rows with `Method: Account` are the only record) and no read endpoint for it exists yet - it needs `CustomerPayment` (Phase 2b) to be subtracted from first.
- 2026-09-15 (Phase 2a, found during live verification): `SaleService.CreateSaleAsync`'s per-line loop now calls `uow.SaveChangesAsync(ct)` immediately after each line's `RecordSaleDepletionAsync` call succeeds, not just once at the end. Without this, a multi-line sale depleting the *same* product/grade twice (e.g. a loose-kg line and a packed line of the same product, spanning batches) got the wrong `CostAtSale` on the second line and could silently over-allocate a batch beyond its physical on-hand: `IStockMovementRepository.GetAvailableBatchesAsync` re-reads on-hand from the database via `AsNoTracking`, not from the DbContext's change tracker, so a second line's FIFO query didn't see the first line's just-added (but still unflushed) `StockMovement` rows and computed availability from a stale snapshot. Caught in this phase's own required verification (check #2: a 12kg line fully draining a 5kg batch, followed by a 1.5kg packed line that should have spilled into the next, more expensive batch - it instead "found" 5kg still sitting in the first batch and priced itself at that batch's cost instead of the correct one). The extra flush stays inside the one transaction `BeginTransactionAsync` already opened, so atomicity is unaffected - a later line's failure still rolls every earlier flush back too (re-verified via direct SQL: a deliberately over-depleting request left zero `Sales`/`SaleLines`/`SalePayments`/`StockMovements` rows behind).
- 2026-09-15 (Phase 2a, found during live verification): `CreateSaleAsync` originally always returned via `CreatedAtAction` (201), even on an idempotent replay - doc 08 explicitly says a replayed `ClientGuid` should return 200 ("do nothing, return the existing sale"), not 201. Fixed by introducing `CreateSaleResult(SaleDto Sale, bool WasReplay)` as `ISaleService.CreateSaleAsync`'s return payload; `SalesController.Create` now answers `Ok(sale)` when `WasReplay` is true and `CreatedAtAction(...)` (201) otherwise, so a retrying offline client can distinguish "this already existed" from "I just created this" the way doc 08 describes.
- 2026-09-15 (Phase 2b): `TillSessionService.CloseAsync` computes `SystemCardTotal` via a new `ISalePaymentRepository.GetCardTotalForTillSessionAsync` - a `SalePayment` join `Sale` query (`WHERE Sale.TillSessionId = @id AND Sale.Status = Complete AND SalePayment.Method = Card`), not a method on `ITillSessionRepository`/`ISaleRepository`. The query's primary aggregate is over `SalePayment` rows, so it lives on that repository (same "repository owns the query whose primary entity it aggregates" reasoning as `IStockMovementRepository.GetOnHandSummaryAsync`), joining `Sale` only to filter. `Status == Complete` is the load-bearing filter that makes day-close correct without any special-casing: a sale refunded within the same session before close is silently excluded, no extra logic needed in `CloseAsync` itself. `GetAccountTotalForCustomerAsync` (added alongside, for `CustomerPaymentService.GetBalanceAsync`, Phase 2b's other half) follows the identical shape - both use the `SumAsync(x => (decimal?)x, ct) ?? 0m` pattern already established by `IStockMovementRepository.GetOnHandAsync`, since `SumAsync` over an empty non-nullable `decimal` sequence throws.
- 2026-09-15 (Phase 2b): `CloseAsync` is a single-entity write (one `SaveChangesAsync`, no `BeginTransactionAsync`) - same "single-entity write, one atomic save" precedent as `OpenAsync`. There's a read (`GetCardTotalForTillSessionAsync`) before the write, but nothing else is mutated in between, so there's no multi-step operation to protect with a real transaction the way `SaleService.CreateSaleAsync`/`RefundSaleAsync` need one.
- 2026-09-15 (Phase 2b): `TillSessionDto` gained the four day-close fields (`SystemCardTotal`, `CardMachineBatchTotal`, `Difference`, `DifferenceNote`) so `POST .../close`'s response (and any later `GET`) can show the computed reconciliation, not just `ClosedAt`. `OpenAsync`'s response naturally carries them as null (nothing writes them until close).
- 2026-09-15 (Phase 2b): `CustomerPayment.Method` is a **new enum** (`CustomerPaymentMethod: Cash|Card|EFT`), not a reuse of `SalePaymentMethod`. `SalePaymentMethod.Account` means "put this sale on the tab" - it describes how a *sale* was paid for, and is meaningless as an answer to "how did the customer pay off their account" (you can't settle a debt by putting it on the same debt). The other two `SalePaymentMethod` values (`Card`/`EFT`) would have fit, but reusing the enum would still expose the nonsensical `Account` value on every `CustomerPayment` write with nothing stopping a client from picking it. A plain `string` was also considered (simplest, "don't over-engineer") but rejected: `Method` genuinely has a small closed set of real values and doc 11's own rule is enums-stored-as-strings for exactly this shape of field. `CustomerPaymentMethod` also adds `Cash` - the POS's card-only decision (doc 01 §4) is specifically about removing float/change/cash-security complexity *at the till*; settling a debt away from the till (a customer arriving with cash, or paying via EFT from home) carries none of those complications, so there's no reason to forbid cash there the way the till does.
- 2026-09-15 (Phase 2b): `CustomerPaymentService.GetBalanceAsync` returns `null` (not a `ServiceResult`/`ServiceError`) when the customer doesn't exist, matching the existing `GetByIdAsync`-returns-nullable convention (`CustomersController.GetById`) rather than introducing a `ServiceResult<CustomerBalanceDto>` for a single not-found case. `CustomersController` calls it directly (`GET /api/v1/customers/{id}/balance`), so the controller now takes both `ICustomerService` and `ICustomerPaymentService` as constructor dependencies - still just "translate HTTP to service call(s)" (doc 11), not business logic living in the controller.
- 2026-09-15 (Phase 2b): balance's "owed" side (`ISalePaymentRepository.GetAccountTotalForCustomerAsync`) and day-close's `GetCardTotalForTillSessionAsync` (see above) both filter `Sale.Status == Complete`, which is *why* refunds (Sale.Status transitions to `Refunded`) matter for balance/day-close correctness without either query needing special-casing for "was this later refunded" - a refunded sale's Account or Card portion simply falls out of both sums the moment `RefundSaleAsync` flips the status.
- 2026-09-15 (Phase 2b): `RefundSaleAsync`'s stock reversal reuses the existing `Adjustment` movement type (`RefTable: "Sale"`/`RefId: saleId`, positive Qty crediting the same `StockBatchId` the original `SaleOut` depleted) rather than adding a new `StockMovementType` enum value (e.g. `SaleReturn`). Reasoning: `StockTake` already uses `Adjustment` for its own bidirectional corrections (positive "found more than expected", negative "found less"), so a refund crediting stock back is the same *kind* of event from the ledger's point of view - a correction against a specific batch, not a new depletion/receipt. The alternative (a dedicated `SaleReturn` type) would read slightly better in a future movement-type breakdown report, but `RefTable`/`RefId` already gives full traceability back to the originating sale without it, and doc 11's own precedent (`StockTake`) is to reuse `Adjustment` for exactly this "known correction against a known batch" shape rather than growing the enum per new correction source. Revisit if reporting ever needs to distinguish "stock-take variance" from "sale refund" adjustments at the aggregate level.
- 2026-09-15 (Phase 2b): the reversal is implemented as `IStockMovementService.ReverseSaleDepletionAsync(saleId, reason, ct)` - the inverse counterpart to `RecordSaleDepletionAsync`, not a new method on `IStockMovementRepository` called directly from `SaleService`. It fetches the sale's original `SaleOut` movements via a new `IStockMovementRepository.GetBySaleDepletionAsync(saleId, ct)` (returns `SaleDepletionMovement(StockBatchId, Qty, LocationId)` records, matching `GetAvailableBatchesAsync`'s "repository returns a plain record list, never a leaked `IQueryable`" convention) and writes one offsetting `Adjustment` movement per original movement, at the *same* `StockBatchId` and magnitude - deliberately never re-running `IStockAllocationService.AllocateFifo`, since a refund is undoing a specific already-known past action, not making a new allocation choice (FIFO could easily pick a *different* batch than the one originally sold from, which would be wrong). Like `RecordSaleDepletionAsync`/`RecordBatchAdjustmentAsync`, it does not call `SaveChangesAsync` itself - `SaleService.RefundSaleAsync` owns the transaction boundary (`BeginTransactionAsync`/.../`CommitTransactionAsync`, the same genuine-EF-Core-transaction pattern `CreateSaleAsync` established in Phase 2a, per the task brief's explicit instruction not to fall back to the older two-phase-save stopgap).
- 2026-09-15 (Phase 2b): `RefundSaleAsync`'s optional `reason` has no dedicated column (doc 02's `Sale` field list has none) - it's folded into `Sale.Notes` (`"Refund: {reason}"`, appended with `" | "` if `Notes` already had content) rather than silently discarded, and also flows into each reversal `StockMovement.Reason` (`"Refund of Sale {id}: {reason}"`) so it's visible from either side of the reversal without a schema change. A dedicated `RefundReason` column would be cleaner long-term if refunds become frequent enough to want to query/report on reasons separately.
- 2026-09-15 (Phase 2b): `RefundSaleAsync` does not validate that the sale's `TillSession` is still open, or that it's being refunded within the same session it was sold in - the task brief's live-verification checklist explicitly exercises refunding a sale *after* its original till session has been closed (and checks a *new* session's day-close correctly excludes it), so refunding across closed/new sessions is expected, normal behaviour, not an edge case to guard against.
- 2026-09-15/16 (Phase 3a): `InputStockMovementType` has three values (`PurchaseIn`/`Consumption`/`Adjustment`) instead of reusing/extending `StockMovementType`'s eleven - input items have no grade/batch/FIFO/sale/transfer concept (they're consumed, never sold or moved between locations), so the produce-side type list would carry eight meaningless values. `InputStockMovement` is otherwise a deliberate analogue of `StockMovement` (append-only, `SUM(Qty)` on-hand, `RefTable`/`RefId` traceability, `IPeriodLocked`) but with `InputItemId` directly on the row instead of a `StockBatchId` - there's no batch layer to point through.
- 2026-09-15/16 (Phase 3a): `GetWeightedAverageCostAsync`'s cost is denormalized directly onto `InputStockMovement.UnitCost` (set on `PurchaseIn` rows only, null on `Consumption`/`Adjustment`) rather than joining back to `InputPurchaseLine` via `RefId`. `RefId`/`RefTable` are a loose, untyped pointer (string + nullable int, no FK constraint) - joining through them for a cost calculation on every consumption check felt fragile (nothing stops `RefTable` drifting from `"InputPurchaseLine"`, and the join adds a table for a query that runs on every `Activity` creation). The small denormalization (repeating `UnitCost` that's also on the purchase line) costs one extra nullable `decimal(18,2)` column and stays internally consistent because it's only ever written once, at `PurchaseIn` creation time, by `InputPurchaseService` - never updated afterward.
- 2026-09-15/16 (Phase 3a): `GetWeightedAverageCostAsync` computes `Σ(PurchaseIn.Qty × UnitCost) / Σ PurchaseIn.Qty` across **every** `PurchaseIn` movement ever recorded for the item, not restricted to some notion of "which specific purchased lots are still on hand" - `InputItem` explicitly has no batch/lot tracking (task brief), so there's no way to know which lots were already consumed without FIFO machinery the brief says not to build. This is the standard moving-weighted-average costing method (as opposed to FIFO/specific-lot costing) and is the same convention doc 02/09 already use for bought-in produce cost - it answers "what does this item cost on average, given everything ever bought" rather than "what do the specific units remaining cost", which is the right question when nothing distinguishes one unit of fertiliser from another.
- 2026-09-15/16 (Phase 3a): `InputPurchasesController`'s `POST` is gated behind `CanManageMasterData`, matching `ProducePurchasesController`'s precedent - recording a purchase invoice (choosing a supplier, entering line costs) reads as stock/master-data administration, not day-to-day farm capture, unlike `Planting`/`Season`/`Activity`/`RainfallLog` which the task brief explicitly ungates. This wasn't spelled out in the brief for input purchases specifically, so it's a judgment call by analogy to the existing `ProducePurchase` precedent rather than an explicit instruction.
- 2026-09-15/16 (Phase 3a): on-hand/weighted-average-cost reads (`GET /api/v1/input-items/{id}/on-hand`, `.../weighted-average-cost`) were added to the existing `InputItemsController` rather than a new controller, per the task brief's own suggestion to check whether `InputItemsController` already exists (it does, from Phase 0b-1) - both return the bare `decimal` (`ActionResult<decimal>`, matching `StockMovementsController`'s plain-value read style elsewhere) and 404 when the item itself doesn't exist, which `IInputItemService.GetOnHandAsync`/`GetWeightedAverageCostAsync` signal by returning `null` rather than a `ServiceResult` (0 is a legitimate on-hand/cost value and can't double as "not found").
- 2026-09-15/16 (Phase 3a): `Season.EndDate` is `NOT NULL` - doc 02's field list for `Season` carries no "NULL" marker (unlike `Planting.EndDate NULL`, which explicitly does), so it's read as a deliberate distinction: a season is a defined production-year window decided up front (e.g. "2026 season, 1 Jul 2026 - 30 Jun 2027"), not an open-ended thing that gets closed off later the way a `Planting` does. In practice this means `CreateSeasonRequest` always requires both dates - acceptable for a small farm's own planning cadence, but worth revisiting if a real workflow wants to open a season without committing to its end date up front.
- 2026-09-15/16 (Phase 3a): neither `Planting` nor `Season` gets an `IsActive` column (task brief's own instruction) or a delete endpoint. `Planting.EndDate NULL` already distinguishes "still growing" from "finished" without a separate active flag, and `Season`'s both-dates-required shape (see above) means there's no equivalent "still open" state to track either. No `DELETE` was added for either entity (only `POST`/`PUT`) - once an `Activity` can reference a `Season` (this same phase) and a `Season` a `Planting`, a hard delete would risk orphaning rows with no `IsActive` fallback to soft-delete into instead; this wasn't explicitly asked for in the brief ("standard CRUD"), so it's a deliberate narrowing worth flagging rather than an oversight.
- 2026-09-15/16 (Phase 3a): `PlantingsController`/`SeasonsController` carry no `[Authorize(Policy = "CanManageMasterData")]` on any verb, including `PUT` - the task brief's instruction ("don't gate these behind CanManageMasterData... any authenticated user, matching TillSession/Sale") reads as a whole-controller stance (day-to-day farm capture), not just the create verb, so update follows the same fallback-only policy as create. `GetAllAsync` takes an optional `blockId`/`plantingId` query filter respectively (matching `ISaleRepository`'s `tillSessionId` precedent) even though the brief didn't explicitly ask for it - a natural, low-cost addition given `Activity`'s own `GET /api/v1/activities?seasonId=` requirement one level down.
- 2026-09-15/16 (Phase 3a): `ActivityType` is master data (Grade-style: unique `Name`, `IsActive` soft-delete) and stays gated behind `CanManageMasterData` on write - the task brief's ungating instruction names `Planting`/`Season`/`Activity`/`RainfallLog` specifically, never `ActivityType`, and it fits the same "who's allowed to define new categories of thing" shape as `Grade`/`Block`/`Crop` rather than "who's allowed to record today's work." `ActivityType.Category` is a plain `string` (not an enum) - the task brief calls this "your call"; a string keeps the set open (adding a new category shouldn't need a code change), unlike `InputItemCategory` which is a genuinely closed, small set.
- 2026-09-15/16 (Phase 3a): `Activity` implements `IPeriodLocked` (`BusinessDate => Date`) - not explicitly asked for in the task brief, but it's a dated, transactional entity of exactly the same shape as `Sale`/`StockMovement`/`InputStockMovement` (all three already implement it), and doc 10 §1's rule ("every transactional insert/update validates its date against the period status") is stated generally, not scoped to specific entities. Leaving `Activity` out would be an inconsistency worth explaining, not a neutral omission, so it's included for consistency with precedent.
- 2026-09-15/16 (Phase 3a): `IActivityService.CreateActivityAsync` reuses `ServiceError.InsufficientStock` (task brief's explicit instruction) with a per-line detail message (`"Input item {id}: requested {qty}, only {onHand} on hand."`) rather than adding a new error case - matches `ErrorResult`'s existing two-arg overload (`ApiControllerBase`) that already renders `InsufficientStock` with a caller-supplied detail, no controller change needed. The whole operation (header insert + every input line's on-hand check/cost snapshot/`ActivityInput`/`Consumption` movement) runs inside one `BeginTransactionAsync`/`CommitTransactionAsync`/`RollbackTransactionAsync` block, per-line `SaveChangesAsync` flushing between lines so a second line consuming the *same* `InputItemId` sees the first line's already-added depletion (re-applying the fix `SaleService.CreateSaleAsync` needed in Phase 2a for the identical reason - `GetOnHandAsync` reads via `AsNoTracking`, not the change tracker) - verified live (see verification section) with a two-line activity over-consuming on its second line: zero `Activities`/`ActivityInputs`/`InputStockMovements` rows survived the rollback.
- 2026-09-15/16 (Phase 3a): `CreateActivityInputLineRequest` carries only `InputItemId`/`Qty` - no `UnitCost` field at all, not even as an ignored/optional one - because doc 02 and the task brief are explicit that the cost is a server-computed snapshot (`GetWeightedAverageCostAsync` at the moment of consumption), never client-supplied; accepting-but-ignoring a `UnitCost` from the request would invite a client to believe it controls the cost. `CreateActivityRequest.Inputs` may legitimately be an empty list - not every activity consumes input stock (pruning, weeding, thinning have no `ActivityInput` lines).
- 2026-09-15/16 (Phase 3a): `RainfallLog` gets a surrogate `RainfallLogId` (int, identity) rather than `Date` itself as the primary key, even though doc 02's field list shows no surrogate id for this table - every other entity in the codebase (including ones with an equally-obvious natural key, like `ActivityType.Name`) uses an int surrogate key with `GetByIdAsync(int id)`-shaped repository/controller plumbing, and matching that shape means `RainfallLogsController` needs no special-casing (no `[HttpGet("{date}")]` route-value-binding wrinkle, no departure from every other controller's `{id:int}` convention). The "one reading per day" natural-key rule is still enforced, just as a unique index on `Date` (`IX_RainfallLogs_Date`) rather than as the primary key itself - `ExistsByDateAsync` backs the duplicate-date rejection the same way `ExistsByNameAsync` backs every other entity's duplicate-name rejection.
- 2026-09-15/16 (Phase 3a): `RainfallLog` supports `PUT` (correcting a mis-entered `Mm`/`Notes` value) but the request excludes `Date` - `UpdateRainfallLogRequest(decimal Mm, string? Notes)` only. `Date` is the natural key the uniqueness rule is built around; allowing it to change on update would mean re-validating uniqueness against a different date and effectively becomes "move this reading to a different day," which reads more like delete-and-recreate than a correction. The task brief left this as "your call" - correcting the value once entered was judged worth supporting (a real, low-risk need); moving which day it belongs to was not (out of scope this phase, no delete endpoint exists to pair with it).
- 2026-09-16 (Phase 3b): the chemical withholding-period lock (doc 05 §5) is split into a pure `FarmApp.Domain.Services.WithholdingLockCalculator` (`SprayWithholding` in, `WithholdingLockResult` out - zero dependencies, unit tested in `WithholdingLockCalculatorTests`, same shape as `StockAllocationService`/`SaleLineCalculator`) and an orchestrating `Api.Application.WithholdingLocks.WithholdingLockService` that resolves which sprays happened on a block and feeds them to the calculator. "Locked" is computed as `harvestDate < MAX(spray.LockedUntil)` across every matching spray, which is mathematically equivalent to "locked by at least one spray" (any individual spray's `LockedUntil` is at most the maximum) while also directly identifying the single most-restrictive spray to name in the human-readable reason (doc 05 §5: "a block could have been sprayed multiple times with different chemicals/windows - take the latest-ending lock"). The boundary is exclusive (`<`, not `<=`) - harvesting on the exact day a withholding window ends is allowed, matching "may not be harvested for N days" read literally.
- 2026-09-16 (Phase 3b): the block-lookup query (`Planting.BlockId == blockId -> Season.PlantingId -> Activity.SeasonId -> ActivityInput.ActivityId -> InputItem` filtered to `Category == Chemical && WithholdingDays != null`, task brief's exact trace) lives as one method, `IActivityInputRepository.GetChemicalSpraysForBlockAsync`, rather than being split across `IPlantingRepository`/`ISeasonRepository`/`IActivityRepository`. Reasoning: the primary aggregate is `ActivityInput` rows (the actual spray events being searched for) - the other tables are joined purely to filter down to one block - matching `ISalePaymentRepository.GetCardTotalForTillSessionAsync`'s established "the repository owns the query whose primary entity it aggregates" precedent (Phase 2b). It's a single LINQ query with four joins, translated to one SQL statement - not four repository round-trips.
- 2026-09-16 (Phase 3b): `GET /api/v1/blocks/{id}/withholding-status?date=` is a read-only pre-check added to the existing `BlocksController` (a second constructor dependency, `IWithholdingLockService`, alongside `IBlockService` - same shape `CustomersController` already uses for `ICustomerPaymentService.GetBalanceAsync`, Phase 2b). `date` is optional and defaults to today (`DateTime.Today`) - a quick "can I harvest here right now" check needs no query param most of the time; a caller planning ahead can still pass a future date. Returns 404 only when the block itself doesn't exist, matching `GetBalanceAsync`'s nullable-not-ServiceResult convention (there's no write to fail here).
- 2026-09-16 (Phase 3b): `Harvest.PickedBy` is left as an optional (`int?`), caller-supplied field, never defaulted from the authenticated user the way `TillSession.OpenedBy` is. Reasoning (task brief explicitly asked for a judgment call here): a harvest is very plausibly logged by someone other than whoever physically did the picking (a farm worker picks all day, the owner or a family member logs it on a phone that evening) - defaulting to the JWT's user would silently misattribute the labour every time that's true, whereas leaving it null when unknown costs nothing. No FK-existence validation is performed against `AppUser` for this field (there's no `IAppUserRepository` exposed to the `Application` layer for such a check - `AppUser` lookups exist only inside `Auth`/`TokenService`) - same "loose, unvalidated FK" treatment already accepted elsewhere in the codebase for fields with no repository to validate against.
- 2026-09-16 (Phase 3b): `CreateHarvestAsync` uses a real EF Core transaction (`BeginTransactionAsync`/`CommitTransactionAsync`/`RollbackTransactionAsync`), matching `ActivityService`/`SaleService` rather than `ProducePurchaseService`'s older two-phase-save stopgap - the task brief explicitly asked for this despite Harvest only *adding* stock (no depleting mid-loop availability check, so no per-line-flush staleness risk the way `ActivityService`/`SaleService` have). The whole header+lines+batches write still needs to roll back together: if a later line's `IStockBatchService.CreateAsync` call somehow fails (defensively - every `ProductId`/`GradeId` was already validated up front), the harvest header and every earlier line/batch/movement already flushed in this call are rolled back too, not just whatever remained unflushed in the change tracker.
- 2026-09-16 (Phase 3b): a dedicated `Harvest.WithholdingOverrideReason` column (nullable `nvarchar(500)`) stores the human's justification whenever a harvest proceeds inside a withholding lock - never folded into `Notes` (task brief's explicit instruction: "the override reason needs to survive as normal queryable data... not buried in generic audit JSON"). It stays `null` on every non-overridden harvest (locked-and-rejected harvests never get created at all; not-locked harvests have nothing to override). A caller supplying a reason when the block *isn't* locked has it silently ignored (never persisted with a stray, meaningless reason attached) rather than rejected - harmless either way, and rejecting it would be an odd, unhelpful failure mode for a client that always sends the field defensively.
- 2026-09-16 (Phase 3b): `StockBatch.HarvestId` points at the `Harvest` header only, not at a specific `HarvestLine` (doc 02's field list, and the task brief's explicit instruction to reuse `IStockBatchService.CreateAsync`'s existing `HarvestId` parameter as-is) - unlike `ProducePurchaseLine`, which has a real `StockBatch.PurchaseLineId` FK pointing at the specific line. This means a harvest with multiple lines can't be traced batch-to-line by a stored key; `IHarvestService.ToDtoAsync` reconstructs the pairing positionally instead - `HarvestLine`s ordered by `HarvestLineId` ascending zipped against that harvest's `StockBatch`es ordered by `StockBatchId` ascending, relying on both having been inserted 1:1 in the same order inside `CreateHarvestAsync`'s loop. This is a known limitation of doc 02's schema for `Harvest` specifically (not present for `ProducePurchase`): it would misattribute cost/batch-id in the returned DTO only if some other process inserted an unrelated `StockBatch` row with the same `HarvestId` out of band, which nothing else in the codebase does. Flagged here rather than silently accepted - a future schema change adding a `HarvestLineId` column to `StockBatch` would close this gap cleanly if it ever matters (e.g. a correction/audit tool that edits batches directly).
- 2026-09-16 (Phase 3b): `GetAllAsync(int? seasonId, ...)`'s optional filter on `HarvestsController`/`IHarvestRepository` wasn't explicitly requested by the task brief (`Harvest`'s controller is just described as "POST/GET /api/v1/harvests") but is added anyway, by direct analogy with `Activity`'s own `GET /api/v1/activities?seasonId=` (Phase 3a) - both entities hang off the same `Season`, and the query costs nothing to add.
- 2026-09-16 (Phase 4a): `CreateHarvestLineRequest.UnitCost` is removed entirely rather than kept-but-ignored. Doc 09's estimate/true-up rule means a harvest batch's cost must always come from `Season.EstimatedCostPerKg`, snapshotted server-side at the moment of harvest - accepting a `UnitCost` field on the request (even if silently overridden) would invite a caller to believe they control the cost, the same reasoning `CreateActivityInputLineRequest` already applied to its own `UnitCost` in Phase 3a. When a season has no `EstimatedCostPerKg` yet (nobody has set `ExpectedTotalCost`/`ExpectedYieldKg`), `CreateHarvestAsync` rejects the harvest outright with `ServiceError.SeasonEstimateNotSet` rather than falling back to any caller-supplied number - the task brief offered a caller-supplied-override fallback as an acceptable alternative, but a hard rejection keeps the "estimate is always the season's own number, never ad hoc" invariant simple and matches the withholding-lock precedent (Phase 3b) of a clear, actionable rejection over a silent workaround. The gap is real (a truly new farm's very first season has no prior-season number to seed the estimate with) but is judged an acceptable one-time friction: the owner types a rough `ExpectedTotalCost`/`ExpectedYieldKg` guess onto the season once, and every harvest after that "just works".
- 2026-09-16 (Phase 4a): `Season.ExpectedTotalCost`/`ExpectedYieldKg`/`EstimatedCostPerKg` are all nullable, and `EstimatedCostPerKg` is always recomputed server-side (`ISeasonCostCalculator.CalculateEstimatedCostPerKg`) from the other two on every `CreateAsync`/`UpdateAsync` call rather than accepted as a request field - doc 09's explicit instruction ("don't trust a client-supplied value that could drift from the two numbers it's derived from"). `UpdateAsync` now rejects with `ServiceError.SeasonAlreadyClosed` once `Season.Status` is `Closed` - doc 09 describes the estimate as "editable while the season is open" and, more importantly, editing `ExpectedTotalCost`/`ExpectedYieldKg` after close would silently move `EstimatedCostPerKg` out from under a true-up that's already been posted against the old value.
- 2026-09-16 (Phase 4a): `ISeasonCostCalculator` (`FarmApp.Domain.Services`) is being built up incrementally across this phase's commits rather than landing whole - `CalculateEstimatedCostPerKg` lands with the `Season` estimate-fields commit (it's what `SeasonService` needs immediately), while the actual-cost-per-kg and true-up arithmetic land with the `SeasonCostSummary`/close-flow commit that actually needs them. Same class, same interface, grown one real caller at a time - consistent with `SaleLineCalculator`/`StockAllocationService`'s "pure, unit-tested Domain calculation" precedent, just introduced in two steps instead of one.
- 2026-09-16 (Phase 4a): `ISeasonCostingService.CloseSeasonAsync` from doc 09's implementation notes is split into `PreviewCloseAsync` (read-only, no writes, callable any number of times while the season is open) and `ConfirmCloseAsync` (the actual posting) - doc 09's own explicit instruction ("a user-confirmed posting... show the number, owner clicks approve"). `POST /api/v1/seasons/{id}/close` returns the same `SeasonCostSummaryDto` it persists, so the HTTP response itself is the confirmation record regardless of whether the caller actually called `GET .../cost-preview` first - the task brief noted this isn't enforceable server-side without extra state, and this sidesteps needing any.
- 2026-09-16 (Phase 4a): the true-up adjustment is stored as a `TrueUpAmount` field directly on `SeasonCostSummary`, not as a separate ledger entity - doc 09 describes it as "one adjustment line," and it's a single number produced 1:1 with (and never meaningful apart from) the summary that computed it, so a dedicated entity would just be a one-row, one-column table joined 1:1 forever. `StockBatch.UnitCost`/`SaleLine.CostAtSale` are never rewritten by `ConfirmCloseAsync` - both stay permanent snapshots (doc 09/02's explicit rule), so the true-up exists solely as this one record, never as a silent revaluation of historical rows.
- 2026-09-16 (Phase 4a): `TrueUpAmount` is computed **per harvest-sourced `StockBatch`** (`Σ(actualCostPerKg - batch.UnitCost) x batch.QtyIn`), not as one `(actualCostPerKg - Season.EstimatedCostPerKg) x TotalKgHarvested` calculation - the task brief offered both as acceptable, noting the per-batch approach is "more precise... if batches used different estimates over time" (`Season.EstimatedCostPerKg` is editable mid-season, so this is a real, not theoretical, case). `SeasonCostPreviewDto.EstimatedCostPerKg` still surfaces the season's *current* estimate for human comparison against `ActualCostPerKg`, but callers should read `TrueUpAmount` itself as the authoritative figure, not re-derive it from those two preview fields when the estimate changed mid-season.
- 2026-09-16 (Phase 4a): the three accumulation queries (`Σ ActivityInput.Qty x UnitCost`, `Σ Activity.LabourCost`, `Σ HarvestLine.QtyKg`, all scoped to a season) live on `IActivityInputRepository`/`IActivityRepository`/`IHarvestLineRepository` respectively, each on the repository whose entity is the primary aggregate being summed - matching `IActivityInputRepository.GetChemicalSpraysForBlockAsync`'s established "the repository owns the query whose primary entity it aggregates" precedent (Phase 3b) - rather than centralizing all three on a new `ISeasonCostingRepository`. `IStockBatchRepository.GetHarvestBatchesBySeasonIdAsync` returns `FarmApp.Domain.Services.HarvestBatchCost` directly (not a generic `Expression<Func<StockBatch, TResult>> selector`) since every real caller of this specific season-scoped query wants exactly those two fields for the true-up calculation - a rare, deliberate departure from the "specific repositories for real questions" pattern's usual generic-selector shape, justified by there being exactly one real question here.
- 2026-09-16 (Phase 4b): `ExpenseCategory` follows the `Grade`-style master-data pattern (unique `Name`, soft-delete `IsActive`, `CanManageMasterData` on writes) even though doc 02's field list shows no `IsActive` - same reading as `Grade`/`Block`/`Crop`, whose own doc 02 entries are equally bare but whose entities all carry it. `Expense` itself is plain create/read only, no update/delete - the task brief offered "maybe allow correcting an obviously-wrong entry" as a judgment call, and the simplest reading of "keep it simple" was taken: an expense is a historical fact once recorded, matching `CustomerPayment`'s precedent, and a wrong entry is corrected the same way a wrong `CustomerPayment` would be (a human either lives with it or a future phase adds an explicit correcting-entry mechanism, not silent mutation). `Expense.Date` is client-supplied (unlike `Sale.DateTime`/`CustomerPayment.Date`, always server-`UtcNow`) because an expense slip is routinely captured after the fact ("found this receipt from last week") - the real transaction date matters for period-locking (`IPeriodLocked.BusinessDate => Date`) and can't be assumed to be "now".
- 2026-09-16 (Phase 4b): Dapper and the report query class (`ReportQueries`) live in `FarmApp.Api` (`Application/Reports/`), not `FarmApp.Infrastructure` - keeping the "reports bypass repositories" exception (doc 11) visibly confined to Api's own Application layer rather than threading a second, raw-SQL data-access mechanism through Infrastructure alongside its normal EF/repository plumbing. `ReportQueries` is a single plain concrete class (constructor-injected `IConfiguration`, no `IReportQueries` interface, no per-report class split) registered directly in DI - every other Application feature pairs an interface with an implementation for testability, but a report query has no second implementation on the horizon and the task brief explicitly warns against `IReportRepository`-style abstractions here; adding an interface anyway would be exactly the "pattern zealotry" doc 11 calls out, just moved one layer up.
- 2026-09-16 (Phase 4b): `CanViewReports` (named in doc 13's policy table, never wired up before now) is added and mapped to `Owner` only, gating all four `ReportsController` actions - the task brief left this as "your call, wire it consistently if you do." Chosen over the deny-by-default fallback because the income statement and sales analysis surface margins/COGS, which read as owner-level financial detail rather than something a Cashier/Worker needs for day-to-day work, unlike `Sale`/`TillSession`'s own operational reads.
- 2026-09-16 (Phase 4b): `Reporting.WastageValue` (feeding the income statement's Wastage line) filters `StockMovement.Type = 'Wastage'` only - not `OwnUse`/`Sample`/`Donation` too, despite the task brief's own "`Type IN (Wastage, ...)`" phrasing leaving the door open. Doc 04 §1 names Wastage specifically for this line; the other three types are deliberate, intentional uses of stock (a sample given to a prospective wholesale buyer, produce donated, stock taken home) rather than loss/shrinkage, and folding them into a line literally labelled "Wastage" would misrepresent what happened to the stock. They remain fully visible in `Reporting.StockMovementSummary`'s per-type ledger if a future report wants to break them out separately.
- 2026-09-16 (Phase 4b): the income statement's `GrossProfit = Sales − Cos − Wastage` (not `Sales − Cos` alone) - doc 04 §1's own layout subtracts Wastage *before* arriving at "Gross profit," and the task brief's `IncomeStatementDto` field list (`Sales, COS, GrossProfit, Wastage, ...`) is read as "Wastage is shown as its own line and IS reflected in GrossProfit," not "folded silently into COS" (which is what the brief's verification note actually warns against - COS itself stays pure `Σ CostAtSale × Qty` for Complete sales, never combined with wastage). `NetProfit = GrossProfit − TotalExpenses` follows directly. Live-verified: Sales 200.00, Cos 100.00, Wastage 30.00 (3kg × R10.00), GrossProfit 70.00 (200 − 100 − 30), Expenses 150.00, NetProfit −80.00 (70 − 150) - all arithmetically consistent and matching hand-calculated numbers exactly.
- 2026-09-16 (Phase 4b): `StockMovementRepository.GetOnHandSummaryAsync`'s Phase 1a client-side-`GroupBy` workaround is **left in place**, not replaced by the new `Reporting.StockOnHand` view, even though the view now exists and computes the identical result (live-verified: both report 87.000 on-hand / R870.00 value for the same product/grade after this phase's test scenario). Reasoning: that method backs `StockMovementsController`'s `/api/v1/stock-movements/on-hand` operational endpoint - a normal EF/repository-layered `Application` service (`StockMovementService`), not a controller under the Reports feature - and reaching into it with Dapper would blur doc 11's line between "reports bypass repositories" (a deliberate, narrow exception) and "everything else goes through EF." The client-side grouping is still fine at this phase's data volumes (documented already in Phase 1a); revisit only if it becomes a real performance problem, at which point the cleanest fix is probably to have that endpoint call `ReportQueries.GetStockOnHandAsync` directly rather than re-inventing a second SQL path.
- 2026-09-16 (Phase 4c): the farming report's per-season row (doc 04 §4) is split across two classes rather than living entirely in `ReportQueries`: `ReportQueries.GetSeasonRevenueAttributedAsync` (pure Dapper, the revenue-allocation approximation) and the new `Application/Reports/SeasonFarmingReportService` (a light EF-backed assembler, constructor-injecting `ISeasonRepository`/`ISeasonCostSummaryRepository`/`IActivityInputRepository`/`IActivityRepository`/`IHarvestLineRepository`/`ISeasonCostCalculator` alongside `ReportQueries` itself). Reasoning (task brief left this as "your call per view"): an OPEN season's costs-to-date must be computed live via the same accumulator queries and `ISeasonCostCalculator` arithmetic `SeasonCostingService.PreviewCloseAsync` already uses (task brief: "don't wait for close") - that's real repository/domain-service access, not a Dapper aggregation, so it can't live in `ReportQueries` without breaking that class's "pure Dapper, no repositories" identity (doc 11). `SeasonFarmingReportService` is a plain concrete class with no interface, matching `ReportQueries`'s own precedent - a report assembler has no second implementation on the horizon.
- 2026-09-16 (Phase 4c): doc 04 §4's revenue-attribution approximation (SaleLine has price, StockMovement has batch/season, the two aren't directly linked) is implemented as: for each Product/Grade, compare how much quantity was depleted from THIS season's harvest-sourced batches against how much was depleted in total over the season's own `[StartDate, EndDate]` window (`Reporting.SaleOutDepletion`, summed with and without the `SeasonId` filter), then allocate that fraction of the window's total `SalesAnalysis` revenue for the same Product/Grade. `Reporting.SaleOutDepletion` joins through to `dbo.Sales` (via `StockMovement.RefTable`/`RefId`, always `'Sale'`/`SaleId` for a `SaleOut` movement - confirmed live) and excludes `Status = 'Refunded'`, matching `Reporting.SalesAnalysis`'s own filter exactly - without this, a refunded sale's depletion would still inflate the total-quantity denominator while its revenue is correctly excluded from the numerator, silently understating the attributed proportion for any product a refund ever touched. Live-verified end to end (Phase 4c, Season 1 "2026 Season"): a fresh product harvested 100kg into the season and sold in full attributed 100% of its R2,000 revenue (2000 × 100/100); a 120kg sale of an existing product that FIFO-depleted 112kg from an older, non-harvest batch and 8kg from this season's harvest batch attributed exactly R240 (R3,600 × 8/120) once the refund-exclusion fix was in place - before that fix, an unrelated already-refunded 4kg sale of the same product was inflating the total-depletion denominator with no matching revenue. This remains a documented approximation (task brief: "doesn't need to be penny-exact") for cases doc 04 §4 explicitly accepts one for, not an audited figure.
- 2026-09-16 (Phase 4c): `Reporting.HarvestSummaryBySeason`'s "vs last season" comparison (doc 04 §4) resolves the prior season via a second, tiny query (`SeasonId` with the same `CropId`, most recent `StartDate` before this season's) rather than a `LAG()` window function inside the view - two simple queries beats one cleverer one for a report this infrequently run, and it keeps the view itself a plain `GROUP BY` with no self-referential logic. `HarvestSummaryRowDto.PriorSeasonKg` is `decimal?`, built from a `Dictionary<(int,int?),decimal?>` rather than a plain `Dictionary<(int,int?),decimal>` - `GetValueOrDefault` on the non-nullable version would silently return 0 for "no prior season exists," indistinguishable from "a prior season existed and genuinely harvested zero" (a real, caught-in-verification bug: the first live check returned `0` instead of `null` for Season 1, which is actually the crop's very first season).
- 2026-09-16 (Phase 4c): the "rainfall vs historical" query (doc 04 §4) hits `dbo.RainfallLogs` directly with two plain aggregate queries (this month's total; `AVG` of the same calendar month's yearly totals across every OTHER year) rather than through a `Reporting` view - a single-table aggregation needs no join, unlike every other view added this phase or in Phase 4b. Returns `HistoricalAverageMm: null, YearsCompared: 0` when no other year has a reading for that month yet (task brief: "return null/empty rather than erroring") - live-verified against the current single-month dataset.
- 2026-09-16 (Phase 4c): debtors aging (doc 04 §5) is split the same way the withholding lock was in Phase 3b: a pure `FarmApp.Domain.Services.DebtorsAgingCalculator` (`DebtLine`s + a payment total in, `OutstandingDebt`s / `DebtorAgingBuckets` out - zero dependencies, unit tested in `DebtorsAgingCalculatorTests`) and `ReportQueries.GetDebtorsAgingAsync` fetching the raw per-customer Account-sale/payment rows and feeding them to it. `ReportQueries` now constructor-injects `IDebtorsAgingCalculator` alongside `IConfiguration` - the one report needing real Domain arithmetic (a customer's payments applied oldest-first against their debts) rather than a SQL aggregation; Dapper still owns fetching the rows, just not the settlement logic. `ApplyPaymentsOldestFirst` trusts the caller's ordering rather than re-sorting internally (kept trivial/testable independent of any date-comparison concern) - `GetDebtorsAgingAsync` explicitly `ORDER BY SaleDate` before handing rows over. A payment exceeding total debt settles everything to zero rather than producing a negative/credit row - doc 04 §5 has no notion of a customer credit balance to carry forward. Only customers with a non-zero outstanding total are returned (a fully-settled customer has nothing to show on an aging report). Live-verified with a purpose-built scenario (a customer with a R500 sale 100 days old, a R300 sale 10 days old, and a single R600 payment): the R500 old debt is settled first, the remaining R100 reduces the R300 newer debt to R200, and that R200 lands in the `Current` bucket (only 10 days old) - matching the brief's own worked example exactly. This scenario needed a direct SQL insert for the two `Sale` rows (backdated `DateTime`) since `CreateSaleRequest` has no client-suppliable date - `Sale.DateTime` is always server-`UtcNow`, so there is no supported API path to create a historically-dated sale for testing.
- 2026-09-16 (Phase 4c): `Reporting.SalePaymentDetail`/`Reporting.AccountSalesDetail` both exclude `Status = 'Refunded'` sales (matching `Reporting.SalesAnalysis`'s own filter, and the same reasoning that motivated fixing `Reporting.SaleOutDepletion` earlier this phase) - a refunded sale's original payment shouldn't count as cash that stayed in the business for the cash flow summary, and a refunded Account-method sale is no longer a debt the customer owes for the aging report. `Reporting.PurchaseCosts` is one `UNION ALL` view combining `ProducePurchaseLine`/`InputPurchaseLine` (tagged by a `Kind` column) rather than two separate views or two separate queries summed in C# - both sides have the identical `Date`/`Amount` shape once joined up to their own header, so one view avoids a third near-duplicate of the same "join line to header, expose Date/Amount" pattern.
- 2026-09-16 (Phase 4c): the cash flow summary (doc 04 §5) is named and shaped exactly as the task brief specified: "cash in" = card/EFT sale payments + all customer debt payments, "cash out" = expenses + produce/input purchase costs. The doc/brief's own "cash flow" label is kept (not renamed) even though every figure is card/EFT/account movement, not literal cash (doc 01 §4's no-cash-handling decision) - `CashFlowDto`'s field names (`CashInCardEft`, `CashInDebtorPayments`, ...) are accurate about what each figure actually is, which the task brief judged sufficient without renaming the report itself. Live-verified against the full dataset: CashInCardEft R6,423.00 + CashInDebtorPayments R10.00 = CashIn R6,433.00; CashOutExpenses R650.00 + CashOutPurchases R4,350.00 (R1,750 produce + R2,600 input) = CashOut R5,000.00; Net R1,433.00 - all matching hand-summed totals from the underlying tables exactly.
- 2026-09-16 (Phase 4c): `Reporting.TillSessionsOverShort`/`TillSessionOverShortDto` keep `OpenedBy` as the raw `AppUser` id with no username join, matching `TillSessionDto`'s own precedent (doc 11, DECISIONS.md Phase 3b: no `IAppUserRepository` is exposed outside `Auth`/`TokenService` for such a lookup). "Per cashier" grouping/ordering (doc 04 §5) is left to the caller/Angular layer - the endpoint returns detail-grain rows ordered by `OpenedBy, ClosedAt`, same "detail grain, let the caller group" precedent as `SalesAnalysis`.
- 2026-09-16 (Phase 4c): month-end close (doc 10 §1) needed no new migration - `AccountingPeriod`'s entity/config/`PeriodLockInterceptor` have existed unexercised since Phase 0a exactly as doc 10 planned ("retrofitting the check into every write path later is painful" - it never needed retrofitting). This phase adds only the missing layer on top: `IAccountingPeriodRepository`/`AccountingPeriodRepository` (standard layering, doc 11 - `AccountingPeriod` reads/writes are real state mutation, not a report), plus one new query method each on `ITillSessionRepository`/`ISeasonRepository`/`IStockTakeLineRepository` (each living on the repository whose entity is the primary thing being surfaced, matching `IActivityInputRepository.GetChemicalSpraysForBlockAsync`'s established precedent), plus `ReportQueries.GetWastageInPeriodAsync` (reusing Phase 4b's `Reporting.WastageValue`) for the informational item.
- 2026-09-16 (Phase 4c): of doc 10 §1's five checklist items, item 2 (outbox) is skipped entirely (no offline sync exists, per the task brief) and item 1 (all till sessions closed) is the **only** hard block - `CloseChecklistDto.CanClose` mirrors `TillSessionsAllClosed` exactly, matching the task brief's explicit instruction ("unambiguous, per the doc"). Items 3-5 are all advisory/informational and are still returned in full on a successful close response (`CloseMonthResultDto` carries both the closed period AND the checklist) so they stay visible even though they didn't block anything - the task brief was explicit about this too.
- 2026-09-16 (Phase 4c): item 3 (stock movement balance check) is implemented as the "more meaningful" of the two interpretations the task brief offered - surfacing `StockTakeLine`s with a non-zero `Variance` recorded in the period (`IStockTakeLineRepository.GetNonZeroVariancesInPeriodAsync`) - rather than re-proving `opening + in - out ± adj = closing`, which is always true by construction of an append-only movement ledger (already noted as not very meaningful on its own in `StockMovementSummaryDto`'s Phase 4b doc comment) and so would never actually catch anything. "Material" is read simply as "non-zero" - there's no "reviewed"/"actioned" flag anywhere in this schema to distinguish a variance a human has already looked at from one they haven't (task brief: "this phase shouldn't invent elaborate workflow state for it"), so every non-zero variance in the period surfaces, every time, until a future phase adds such a flag if it's ever needed.
- 2026-09-16 (Phase 4c): item 4 (open seasons reviewed / closed seasons true-up posted) reads "closed within this period" as **the season's own `EndDate` falling in the period**, not "whose `SeasonCostSummary.ClosedAt` falls in the period" - `Season` carries no close-timestamp of its own (only `SeasonCostSummary.ClosedAt` records when the true-up was actually posted, which could legitimately lag the season's real end by days or weeks), and defining "closed within this period" via the summary's own timestamp would make the "flag if somehow missing" check structurally unable to ever find a violation (querying *through* the summary to find seasons closed in the period necessarily only finds seasons that already have one). Reading it via `EndDate` instead means a season whose farming year genuinely ended in this period, is marked `Closed`, but somehow has no `SeasonCostSummary` row is a real, catchable data-integrity gap - even though the only write path in this codebase (`SeasonCostingService.ConfirmCloseAsync`) creates the summary and flips `Status` atomically in the same save, so this defends against a hypothetical future code path or manual data edit, not a bug that can currently occur (same "not very meaningful given append-only construction, but real integrity value" caveat doc 10 itself applies to item 3).
- 2026-09-16 (Phase 4c): item 5 (wastage entries reviewed) is implemented as purely informational - `CloseChecklistDto.WastageEntriesForReview` always populates, never affects `CanClose`, and there is no "reviewed" boolean anywhere (task brief: "the checklist's job here is surfacing the list, not gatekeeping it"). It reuses `Reporting.WastageValue` (Phase 4b) via a new `ReportQueries.GetWastageInPeriodAsync`, period-filtered instead of date-range-filtered - same underlying view, no new SQL object needed.
- 2026-09-16 (Phase 4c): all three `AccountingPeriodsController` actions (checklist, close, reopen) are gated behind `CanManageMasterData` (Owner), not just Reopen - the task brief only explicitly named a policy for Reopen ("Owner role only... doc 13 doesn't define a more specific one and this phase shouldn't invent a new policy for a single action"). Extended to checklist/close too because month-end close is the same class of financial-control action end to end, not an operational read a Cashier/Worker would need day-to-day (same reasoning Phase 4b applied to `CanViewReports`) - a Cashier being able to see or trigger a month-end close would be an odd permission boundary, not a deliberately narrower one.
- 2026-09-16 (Phase 4c): `CloseMonthAsync`'s `closedBy`/`AccountingPeriodsController.Close`'s user-identity resolution uses `User.FindFirstValue(ClaimTypes.Name)` (the JWT's username claim), not `ClaimTypes.NameIdentifier` (the user id) - `AccountingPeriod.ClosedBy` is a `string` column (doc 10 §1's own field list: `ClosedBy` next to `Year`/`Month`/`Status`, not an int FK), unlike `TillSession.OpenedBy`/`Harvest.PickedBy` which are int `AppUser` ids. Live-verified: `ClosedBy: "andri"` after closing via the seeded Owner account's JWT.
- 2026-09-16 (Phase 4c): **the period-lock end-to-end proof** (task brief's highest-priority verification item) was run for real, for the first time in this codebase's history, against September 2026 (the month essentially all of this project's live-verification test data happens to fall in): (1) checklist showed 2 open till sessions (`#1`, `#4`) and `canClose: false`; (2) `POST .../close` was rejected 409 `AccountingPeriodTillSessionsOpen` while they were open; (3) both were closed via `POST /api/v1/till-sessions/{id}/close`; (4) the checklist re-ran clean (`tillSessionsAllClosed: true`, `canClose: true`, with the three advisory lists still populated and visible); (5) `POST .../close` succeeded, returning `Status: "Closed"`, `ClosedBy: "andri"`; (6) a `POST /api/v1/harvests` dated `2026-09-25` (inside the now-closed month) was rejected - genuinely, not just superficially: the server logged `System.InvalidOperationException: Cannot save: 2026-09 is a closed accounting period.` thrown from `PeriodLockInterceptor.SavingChangesAsync`, `HarvestCount`/`StockBatchCount` were unchanged before and after (4 and 13 respectively - `HarvestService`'s existing `BeginTransactionAsync`/`RollbackTransactionAsync` wrapping correctly rolled back the whole operation), and the HTTP response was a 500 (unhandled - see the next entry); (7) the identical request dated `2026-10-01` (the next, open period) succeeded normally (`201`, new `HarvestId`/`StockBatchId`); (8) `POST .../reopen` with an empty reason was rejected `400` by `ReopenMonthRequestValidator`, then succeeded with a real reason, returning `Status: "Open"` while preserving the original `ClosedAt`/`ClosedBy` and populating `ReopenedAt`/`ReopenReason`; (9) the exact same `2026-09-25` harvest request, retried unchanged, now succeeded (`201`); (10) `AuditLogs` confirmed both the original close (`Insert`, `AccountingPeriod`) and the reopen (`Update`, `Changes: {"Status":{"Old":1,"New":0},"ReopenedAt":{...},"ReopenReason":{...}}`) were captured automatically by the pre-existing `AuditInterceptor`, with no code written this phase to make that happen - doc 10 §1's "reopening is... audit-logged" requirement was already satisfied by Phase 0a's cross-cutting interceptor. The interceptor needed no fix - it worked correctly the first time it was ever actually exercised.
- 2026-09-16 (Phase 4c): a write rejected by `PeriodLockInterceptor` currently surfaces as a bare `500` via the existing generic `ExceptionHandlingMiddleware` (it catches `Exception`, not anything more specific), not a polished `409 Conflict` the way every other business-rule rejection in this codebase does via `ServiceResult`/`ServiceError`. This is a known, real gap - flagged rather than silently accepted - but left as-is this phase: the task brief's verification bar was "confirm it genuinely rejects the write... if it doesn't work, fix it" (a correctness bar, which it meets - the write is genuinely and completely rejected, nothing is persisted), not "make its error response consistent with the rest of the API" (a polish bar, out of this phase's explicit scope). The clean fix, whenever it's picked up: a dedicated exception type the interceptor throws instead of a bare `InvalidOperationException`, caught by `ExceptionHandlingMiddleware` (or a controller-level filter) and mapped to `409` with a message naming the closed period - every write path already funnels through this one interceptor, so the fix is one place, not one per `IPeriodLocked` entity.
- 2026-09-16 (Phase 5a): the actual API port/scheme differs from what the task brief assumed ("https://localhost:5027"), which conflates the two real listeners: `launchSettings.json`'s "https" profile serves `https://localhost:7137` **and** `http://localhost:5027` simultaneously. Verified for real via `dotnet run`'s own console output, not assumed.
- 2026-09-16 (Phase 5a): the Angular dev server (`http://localhost:4200`) talks to the API over **plain HTTP on :5027**, not HTTPS on :7137, in `environment.development.ts` - a deliberate dev-only choice, not an oversight. The API's self-signed HTTPS dev cert is untrusted on this machine, and `dotnet dev-certs https --trust` requires an interactive GUI "trust this certificate?" confirmation that a non-interactive automation session cannot click through (confirmed: it hung waiting on the dialog; `certutil`/`Import-Certificate` into `CurrentUser\Root` were also tried and both refused non-interactively with "UI is not allowed in this operation" - Windows requires a human click to add to the trusted root store, and that boundary was respected rather than forced). Paired with this, `Program.cs`'s `app.UseHttpsRedirection()` was scoped to `!app.Environment.IsDevelopment()` - without that change, the :5027 listener 307-redirects every request to :7137, and a browser refuses to follow a redirect during a cross-origin CORS preflight (OPTIONS), which would break the FE entirely in dev regardless of the cert issue. Verified both the CORS preflight (`curl -X OPTIONS`, real browser `fetch()` from the `:4200` origin) and the full login flow work cleanly over `:5027` post-fix. Production keeps the HTTPS redirect; only Development is affected. `environment.ts` (the prod placeholder) still points at `https://localhost:7137/api/v1` as a reminder that a real prod API origin is needed before deploy - it's never read by `ng serve`.
- 2026-09-16 (Phase 5a): the JWT's claim keys are **not** the short names (`sub`/`unique_name`/`role`) the retired frontend guide's example assumed - verified for real by decoding an actual token returned from `POST /api/v1/auth/login`, not by reading `TokenService.cs` and guessing. `TokenService.CreateAccessToken` builds claims via `new Claim(ClaimTypes.NameIdentifier, ...)`/`ClaimTypes.Name`/`ClaimTypes.Role` passed directly into `new JwtSecurityToken(claims: ...)` and `JwtSecurityTokenHandler().WriteToken(token)` - bypassing `ClaimsIdentity`/`SecurityTokenDescriptor` entirely, which is the only code path `JwtSecurityTokenHandler`'s short-name outbound claim mapping applies to. The result: the JWT payload's actual keys are the full legacy WS-Federation URIs - `http://schemas.xmlsoap.org/ws/2005/05/identity/claims/nameidentifier`, `.../name`, and `http://schemas.microsoft.com/ws/2008/06/identity/claims/role`. `web/farm-app/src/app/core/auth/auth.models.ts` names these as constants (`JWT_CLAIM_USER_ID`/`JWT_CLAIM_USER_NAME`/`JWT_CLAIM_ROLE`) rather than hardcoding the strings inline, and maps them to a small `CurrentUser { userId, userName, role }` shape the rest of the FE actually uses.
- 2026-09-16 (Phase 5a): the token-refresh-and-retry interceptor path was verified against ASP.NET Core's **default 5-minute `ClockSkew`** tolerance on `TokenValidationParameters` - a first verification attempt (20-second access-token expiry, ~26s real wait, confirmed via `Date.now()` before/after, not assumed) found the "expired" token still worked with a bare `200 OK`, no 401, because the default 5-minute skew absorbs a token that's only tens of seconds past its `exp`. Temporarily setting `ClockSkew = TimeSpan.Zero` (alongside the already-temporary 20-second expiry) made the real 401 appear and reproduced the full interceptor cycle. Both temporary changes (`TokenService.cs`'s `AddSeconds(20)`, `Program.cs`'s `ClockSkew = TimeSpan.Zero`, and a temporary "Ping /grades" button added to `DashboardPageComponent` to fire an authenticated call through the app's real `HttpClient`/interceptor without needing a Phase-5b screen to exist yet) were reverted immediately after the observation below, confirmed via `git diff`/`git checkout` showing a clean tree, and the backend was rebuilt + `dotnet test` rerun green before restarting normally.
- 2026-09-16 (Phase 5b-1): `InputItemsController` (and its Phase-5b-2 siblings `ExpenseCategoriesController`, `ActivityTypesController`, `StockBatchesController`) left `[Route("api/v1/[controller]")]` on the default `[controller]` token instead of overriding it with an explicit kebab-case route the way every multi-word sibling controller does (`TillSessionsController` → `api/v1/till-sessions`, `ProducePurchasesController` → `api/v1/produce-purchases`, `StockMovementsController` → `api/v1/stock-movements`, etc.) - confirmed by reading every controller's `[Route]` attribute, not assumed. The real, working route for input items is therefore `api/v1/InputItems` (PascalCase, matching only case-insensitively - ASP.NET routing's default - with a kebab spelling, never literally). This contradicts doc 11's own stated API convention ("Routes: `/api/v1/{resource}` plural kebab-case") but is what the verified, already-shipped backend actually serves. Per this phase's task brief ("never touch... backend `src/`... unless you find a genuine bug blocking you"), this wasn't blocking - the frontend's `InputItemsApiService` just points at the real path (`environment.apiUrl + '/InputItems'`, commented inline) rather than the backend being changed. Flagging here rather than silently routing around it: whoever picks up Phase 5b-2's `ExpenseCategoriesController`/`ActivityTypesController`/`StockBatchesController` screens will hit the identical mismatch and should make the same call (or, better, fix all four controllers' routes in one small pass and update every affected frontend service at once, since by then the inconsistency is proven, not hypothetical).
- 2026-09-16 (Phase 5b-1): `Cultivar`'s crop `<select>` deliberately lists **all** crops, active and inactive, labeling an inactive one `"<name> (inactive)"` rather than filtering it out - a judgment call, not a DTO fact. Reasoning: `CropsApiService.getAll(true)` is called once so the table can always resolve a cultivar's crop name (including one whose crop was since deactivated), and reusing that same full list for the `<select>` keeps editing an existing cultivar assigned to a now-inactive crop possible without a separate code path - the alternative (filtering the create form to active-only crops but the edit form to active+current) added a second signal and a special case for one screen's dropdown, for a farm-management app where "how many active crops" realistically stays small enough that showing the inactive ones, clearly labeled, is not confusing.
- 2026-09-16 (Phase 5b-1): `InputItem`'s `withholdingDays`/`activeIngredient` fields are always visible and editable regardless of `category`, with a CSS opacity de-emphasis (`.de-emphasized`, `opacity: 0.5`) plus a hint line when `category !== 'Chemical'` - not hidden via `@if`, per the task brief's explicit "don't hard-block setting them for other categories (the backend doesn't either)." Hiding the inputs outright when category isn't Chemical would have made them impossible to set from the UI even though the backend's `CreateInputItemRequestValidator`/`UpdateInputItemRequestValidator` place no such restriction (only `GreaterThanOrEqualTo(0)` when a withholding value is present, regardless of category) - de-emphasizing rather than removing keeps the UI honest about what the API actually allows.
- 2026-09-16 (Phase 5b-1): a shared `shared/http-error.util.ts` (`extractErrorMessage(err: HttpErrorResponse)`) and `shared/styles/list-page.scss` were added and reused by all six screens, rather than duplicating the same ProblemDetails-parsing logic and table/form CSS six times. Not called for explicitly by doc 11, but doc 11's own "features/{components, services, models}... shared/ reusable dumb components, pipes" structure already names `shared/` as the place for exactly this kind of cross-feature reuse, and six near-identical copies of the same error-extraction logic would have been the first thing a future pass flagged as duplication.
- 2026-09-16 (Phase 5b-2): `PriceListsController` and `PackSizesController` were **fixed** (not worked around), unlike the Phase 5b-1 precedent of pointing the frontend at whatever casing the backend actually served (see the entry above). The Phase 5b-2 task brief explicitly stated route casing had "just been fixed" for `ExpenseCategoriesController`/`ActivityTypesController` and that every other multi-word controller already matched - but live verification (both GET and POST 404'ing against `/api/v1/price-lists` and `/api/v1/pack-sizes` the moment their screens were wired up) showed `PriceListsController`/`PackSizesController` were missed: both still carried the bare `[Route("api/v1/[controller]")]` token, which substitutes the PascalCase class-name prefix (`PriceLists`/`PackSizes`) verbatim and never matches a kebab-case request path no matter how case-insensitive ASP.NET Core's routing is (the mismatch is the literal hyphen, not casing - `InputItems`, by contrast, is a single dictionary word once you ignore the hyphen point, which is moot since it has none). Since fixing this properly (matching the already-established, already-fixed pattern) was a two-line, zero-risk change directly blocking two of this phase's six required screens, it was fixed in `src/` rather than silently routed around client-side a second time - per the task brief's own instruction that a genuine blocking bug gets fixed minimally and flagged, not worked around. `InputItemsController`/`StockBatchesController` (named in the Phase 5b-1 entry above as having the same latent issue) were deliberately left untouched here - out of this phase's scope (no screen in this batch calls them) and not blocking anything.
- 2026-09-16 (Phase 5b-2): `PackSize`'s "deactivate" step in the task brief's own verification checklist doesn't apply literally - the backend has no `IsActive` on `PackSize` at all (Phase 0b-2 decision, above: pack sizes are hard-deleted, not soft-deactivated, since a wrong one is small enough to just delete and recreate). `PackSizePageComponent`'s table therefore has no "Show inactive" toggle, no Status column, and a single permanent "Delete" action (with its own more serious confirm wording: `` `Delete pack size "${name}"? This cannot be undone.` ``) instead of Deactivate/Edit's isActive checkbox - matching what the backend can actually do rather than adding a client-side-only soft-delete illusion on top of a real hard-delete endpoint.
- 2026-09-16 (Phase 5b-2): `ProductPageComponent`'s conditional fields (`cropId` shown+required only when `productType === 'Produce'`, `makeMode` shown+required only when `'Prepared'`) are enforced by dynamically enabling/disabling those two `FormControl`s (via `productType`'s `valueChanges`) rather than just conditionally rendering the `<select>` with static validators - Angular excludes a disabled control from `form.invalid`, so switching `productType` away from `Produce` both hides the crop dropdown *and* stops it from blocking submission, and the control's value is explicitly reset to `null` on disable so a stale crop/make-mode selection from a previously-chosen type is never silently submitted after switching types (even though the backend itself doesn't hard-restrict `CropId`/`MakeMode` to their "natural" `ProductType` per the Phase 0b-2 decision above - the UI's own conditional-required behavior is a usability choice on top of that permissive backend, not a mirror of a backend constraint).
- 2026-09-16 (Phase 5c-1): a minimal `Location` master-data screen (`features/locations`) was added even though it wasn't named in this phase's task list - `LocationsController` already existed (from an earlier stock-movements phase) but had no frontend screen at all, and Transfer's `FromLocationId`/`ToLocationId` are required, non-nullable fields, so Transfer would have been untestable (and every other movement's optional `LocationId` unusable) without some way to create locations short of Swagger. Kept to the exact same minimal shape as `Grade` (name + IsActive, no other fields) since a location for this app is just a named place, nothing more.
- 2026-09-16 (Phase 5c-1): the seven stock-movement-recording screens (Wastage, OwnUse, Sample, Donation, Adjustment, Repack, Transfer) are ONE shared, route-parameterized component (`MovementFormPageComponent`, driven by a `MOVEMENT_KINDS` config table and the `:kind` route param) rather than seven separate components - the task brief left this as an explicit judgment call. The six single-location types share `RecordStockMovementRequest`'s exact shape and only differ in label/description/POST path; Transfer alone needs different fields (`FromLocationId`/`ToLocationId` instead of one `LocationId`), handled with the same conditional-enable/disable-and-clear pattern `ProductPageComponent` already established for its type-conditional fields (see the Phase 5b-2 entry above), reapplied per movement kind on `ActivatedRoute.paramMap` changes (not `route.snapshot`, since navigating between two `/stock-movements/:kind` cards reuses the component instance - same reuse-strategy trap noted in the entry below, fixed here from the start).
- 2026-09-16 (Phase 5c-1): the UI requires a `Reason` for the six single-location movement types (Wastage/OwnUse/Sample/Donation/Adjustment/Repack) even though `RecordStockMovementRequestValidator` only enforces `MaxLength(200)` server-side, not `Required` - recording *why* stock left matters for this kind of entry (a farmer reviewing history later needs to know it wasn't just "gone"), so the stricter rule is a UI-only usability choice layered on a technically-permissive backend, same pattern as the Phase 5b-2 `ProductPageComponent` decision above. Transfer's `Reason` stays optional in the UI, matching the backend exactly (a transfer's "why" is usually self-evident from the two locations named).
- 2026-09-16 (Phase 5c-1): the stock-on-hand view fetches each batch's derived on-hand quantity via one `GET /stock-batches/{id}/on-hand` call per batch (`forkJoin`over the batch list) rather than adding a new bulk backend endpoint - `IStockMovementRepository` only exposes a product/grade-level summary (`GetOnHandSummaryAsync`, behind `GET /stock-movements/on-hand`), not a per-batch bulk query, and inventing a new backend shape for this screen's convenience felt like exactly the kind of unauthorized shape-guessing the task brief warned against ("check what actually exists... don't invent a different one"). N+1 is fine at this app's real scale (a small farm's count of currently-open batches, verified in the dev DB at a dozen or so rows) - flagged here in case batch volume ever grows enough to matter.
- 2026-09-16 (Phase 5c-1): `ProductRecipePageComponent` subscribes to `ActivatedRoute.paramMap` rather than reading `route.snapshot.paramMap` once in `ngOnInit` - caught live during browser verification: Angular's default route-reuse strategy keeps the same component instance alive when navigating directly between two `/products/:id/recipe` routes (same route config, different `id`), so a snapshot-only read showed the *first* product's recipe after jumping straight to a second one without an intervening `/products` visit. Fixed before this phase's own report was written, not left as a known issue - the same reactive-subscription pattern `MovementFormPageComponent` already needed for its `:kind` param (see above) turned out to apply here too, for the identical reason.
- 2026-09-16 (Phase 5c-1): the recipe editor's ingredient dropdown offers every active `InputItem` regardless of `category` (Seed/Fertiliser/Chemical/Packaging/Ingredient/Other), not just `Ingredient`-category items - `RecipeLineRequestValidator` only checks `InputItemId > 0` and existence, with no category restriction, matching this app's now-consistent "the backend doesn't hard-restrict category-to-purpose pairings, so the UI doesn't invent one either" stance (same reasoning as `ProductPageComponent`'s CropId/MakeMode and `InputItemPageComponent`'s withholding fields, both above). The dev database's existing Cappuccino recipe (used for round-trip verification) in fact already references a `Chemical`-category item, which is a pre-existing test-data quirk, not something this phase introduced or needed to correct.
- 2026-09-16 (Phase 5c-2): the produce-purchase create form and detail view are split into three routes/components (`produce-purchases` list, `produce-purchases/new` create, `produce-purchases/:id` detail) rather than one combined screen - the task brief asked for a create form plus "a simple list/detail view", and `ProducePurchaseDto`'s lines already carry everything a read-only detail view needs (including each line's resulting `StockBatchId`), so a separate detail route reads as a natural "receipt" for a specific purchase rather than forcing the create form to double as both. The create form itself still shows an inline post-submit success panel (line-by-line "created batch #N for line N", matching the task brief's example wording exactly) rather than only redirecting to the detail page, since that immediate feedback is the more useful moment for a farmer to see which batch numbers just got created.
- 2026-09-16 (Phase 5c-2): the stock-take start page lists **every** stock batch (all products, all locations) rather than filtering to the chosen location - confirmed by reading `StockBatchesController`/`IStockBatchService`: `GET /stock-batches` takes no filter parameters at all, and `StockBatchDto` itself carries no `LocationId` (a batch isn't location-scoped; only individual `StockMovement` rows optionally are). The task brief explicitly allowed this fallback ("filtered to the chosen location if the backend supports that, otherwise just list all and let the user pick"), so no backend change was made for it. Zero-on-hand batches are hidden by default (with a "Show batches with zero on hand" toggle), reusing the exact convention `StockOnHandPageComponent` already established in Phase 5c-1.
- 2026-09-16 (Phase 5c-2): a stock-take line's `CountedQty` input starts **blank**, not pre-filled with the already-known `SystemQty` - a deliberate choice to make a farmer physically type the number they counted rather than risk silently accepting the system's own guess by leaving a pre-filled field untouched, which would defeat the point of a stock take.
- 2026-09-16 (Phase 5c-2): once a stock-take line has a non-null `CountedQty`, the count page locks it to read-only (with its variance shown) and only ever sends **still-pending** lines in a `RecordCountsRequest` - this is a correctness fix around a real backend gap, not just a UI nicety. Read `StockTakeService.RecordCountsAsync` closely: it recomputes `Variance = CountedQty - SystemQty` and calls `RecordBatchAdjustmentAsync` for every line in the request with **no check that the line was already counted** - resubmitting the same line a second time (even with an identical value) would fire a second `Adjustment` movement for the same variance, silently double-adjusting that batch's on-hand. Locking counted lines client-side is what makes both "don't double-submit" and "resume counting later, batch by batch" safe at the same time; a real idempotency guard belongs in `RecordCountsAsync` itself (e.g. reject/no-op a line whose `CountedQty` is already set) but that's a backend change outside this phase's scope - flagged here rather than fixed, since the UI-side lock fully closes the practical risk for the one client that exists.
- 2026-09-16 (Phase 5c-2): produce purchases and stock takes are both gated to `hasRole('Owner')` in the UI (`canManage`), matching `[Authorize(Policy = "CanManageMasterData")]` on `ProducePurchasesController.Create`/`StockTakesController.Start`/`StockTakesController.RecordCounts` - verified directly against the controllers and `Program.cs`'s policy registration (`CanManageMasterData` → `RequireRole("Owner")`) rather than assumed from the Phase 1b brief's naming, per this project's repeated "verify, don't trust old summaries" lesson.
- 2026-09-16 (Phase 5d-1): unlike every prior Phase 5 screen, `PosPageComponent` has **no** `canManage`/`hasRole('Owner')` gate - verified directly against `SalesController`/`TillSessionsController`: neither carries `[Authorize(Policy = "CanManageMasterData")]`, so both fall through to `Program.cs`'s fallback policy (`RequireAuthenticatedUser()`, any logged-in user). Matches the task brief's explicit statement and doc 01's real-world framing (a cashier, not just the owner, runs the till) - confirmed rather than assumed, per this project's repeated "verify the actual policy" lesson (Phase 5c-2 entry above).
- 2026-09-16 (Phase 5d-1): the basket is a plain `signal<BasketLine[]>`, not a `FormArray` - the task brief left this as an explicit judgment call. A `FormArray` fits `ProducePurchaseFormPageComponent`'s line editor because every row is typed directly into table cells from the start; the POS basket is instead built by tapping a product card, filling a small picker overlay, and only then having a row exist at all, so there's nothing to bind a `FormArray` control to until after the fact. Each line also carries denormalized display fields (`productName`/`gradeName`/`packSizeName`) resolved once at add-time rather than re-joined against the catalog on every render, since the catalog itself doesn't change mid-sale.
- 2026-09-16 (Phase 5d-1): the default price list is picked by name match (`isActive && name.toLowerCase().includes('retail')`), falling back to the first active price list - the real seeded data has exactly one price list literally named "Retail Pricing" plus a "Wholesale" one, and the task brief explicitly asked for "a Retail one if it exists, check real data" rather than a fixed ID or a backend-side "default" flag (no such flag exists on `PriceListDto`). Switching to Wholesale for a wholesale sale is a manual dropdown next to the till-session bar, applied to future price lookups only - already-added basket lines keep whatever price they were added at, matching how a real till works (you don't retroactively reprice items already rung up).
- 2026-09-16 (Phase 5d-1): a selected `Customer`'s own `PriceListId` (e.g. `Jan van der Merwe` → Wholesale) is deliberately **not** auto-applied to the POS's active price list - Customer is chosen at checkout, after the basket (and its prices) already exist, so retroactively swapping the price list at that point would either silently reprice already-added lines or do nothing visible, both confusing. The cashier picks the right price list up front (or switches mid-basket before adding more lines) instead; auto-linking is a reasonable enhancement for a later phase once there's a real workflow signal for whether cashiers select the customer before or after building the basket.
- 2026-09-16 (Phase 5d-1): `Grade` is offered in the product picker only when `productType === 'Produce'` (same reasoning `ProducePurchaseFormPageComponent`/`MovementFormPageComponent` don't apply, since grade genuinely is a produce-only concept per doc 01) - `GradeId` stays fully optional even for Produce (a "No grade" option), since the backend doesn't require one.
- 2026-09-16 (Phase 5d-1): per-line discount amount/reason live in the **basket row**, not in the picker overlay - the picker's job is "get this item into the basket fast" (task brief's "used with dirty hands in a hurry"), and a discount is a checkout-time judgment call ("this customer is a regular", "this punnet is a bit old") a cashier makes while reviewing the basket, not while grabbing a product off the grid.
- 2026-09-16 (Phase 5d-1): a single payment row's `amount` auto-tracks the basket total as it changes (`syncSinglePayment`, called after every basket mutation) so the common case - one payment method covering the whole sale - never needs the cashier to retype the total after adding a discount or another line. The moment a second row exists (a real split), amounts become entirely the cashier's own input; auto-sync never touches a multi-row split, since there's no single correct way to redistribute a change in total across methods automatically.
- 2026-09-16 (Phase 5d-1): the `ClientGuid` lifecycle (doc 08) is driven entirely by basket state, not by component lifecycle or submit attempts: `ensureClientGuid()` generates one the moment the first line lands in an empty basket, and it's cleared back to `null` if the basket is fully emptied again before checkout (so a later, unrelated basket gets its own id). Only a *genuine success* in `submit()`'s response handler rotates it for the next sale - every error path (network failure, `PaymentMismatch`, `InsufficientStock`, `TillSessionClosed`, validation) leaves it untouched on purpose, since none of those leave a `Sale` row behind (confirmed by reading `SaleService.CreateSaleAsync`: the `ClientGuid` existence check runs before any write, and every failure path either returns before the transaction opens or rolls it back), so resubmitting the same `ClientGuid` after any of them is always a fresh, safe create attempt, never treated as a duplicate.
- 2026-09-16 (Phase 5d-1): `SalesApiService.create` uses `observe: 'response'` instead of the usual body-only `Observable<T>` every other `*ApiService` uses - the one place this phase actually needs the HTTP status code itself (201 vs 200) rather than just the body, to implement doc 08's "tell a genuine create apart from an idempotent replay" requirement (`SalesController.Create` already returns exactly this distinction server-side, confirmed by reading it).
- 2026-09-16 (Phase 5d-1): on a 409 Insufficient stock, the basket line(s) actually responsible are found via a **client-side best-effort reconciliation**, not by parsing the server's error text further. Reading `StockMovementService.RecordSaleDepletionAsync` closely: it prefixes the detail with `"Product {productId}: "` (e.g. `"Product 6: Requested 5, only 0.500 on hand for this product/grade."`) - more specific than the task brief assumed at the DTO level, but still no grade or basket-line identity, and multiple lines can share a product/grade. `flagShortLines()` instead re-fetches `GET /stock-movements/on-hand` and re-walks the basket in the same order/grouping the backend allocates (cumulative per product+grade) to find the first line(s) that would overrun - shown as a red highlight + inline note per line, while the raw server message is also shown verbatim above the basket. Live-verified: a 5kg loose line against Test Plums' real 0.5kg on-hand was rejected with `"Product 6: Requested 5, only 0.500 on hand..."`, the line was highlighted, the basket was untouched, and removing the line cleanly restored the checkout section - confirmed via `GET /sales` that no `Sale` row was created for the rejected attempt.
- 2026-09-16 (Phase 5d-1): the network-failure branch (`err.status === 0`, doc 08's "request never reached the server") was proven against the real running app, not just reasoned about - a temporary one-shot `XMLHttpRequest.open` hijack (injected via the browser's console for this test only, redirecting exactly one `POST /sales` call to an unreachable address, then automatically un-arming itself) forced a genuine connection failure on the first submit of a real basket. The UI showed the calm retry message and kept the basket + `ClientGuid` (`3151226b-4b46-4759-91d6-622f59aeb61c`, read directly off the live component instance via Angular's `ng.getComponent` dev API). Tapping Submit again went to the real backend and succeeded as `Sale #15`, **201 Created** (a genuine first create, since the hijacked attempt never reached the server at all) - `GET /sales/15` confirmed its `ClientGuid` matched exactly, and `GET /sales` showed no duplicate/orphaned sale from the failed attempt. No source was changed to run this test; the hijack lived only in the browser tab's JS context for the duration of the check.
- 2026-09-16 (Phase 5d-1): `pos-page.component.scss` initially exceeded the `anyComponentStyle` build budget (4.37 kB against a 4 kB warning threshold, from `angular.json`) - trimmed by consolidating four near-duplicate `select`/`input`/`label` style blocks (open-form/picker/checkout/receipt) into one shared rule and dropping a couple of small single-use blocks (`.price-list-picker select`'s custom sizing, `.picker-actions button.secondary`'s duplicate of the page-level `button.secondary` rule) rather than raising the budget - the screen is legitimately the largest/most complex in the app (five distinct panels: gate, grid+picker, basket, checkout, receipt), but the four style blocks really were near-identical copies worth deduplicating on their own merits, not just to satisfy a number. `ng build` now completes with no warnings.
