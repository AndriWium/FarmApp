export const environment = {
  production: true,
  apiUrl: 'https://localhost:7137/api/v1',
  // NOTE: this production placeholder is not used by `ng serve` (see environment.development.ts
  // for the dev value actually in effect) and will need a real prod API origin before deploy.
};
